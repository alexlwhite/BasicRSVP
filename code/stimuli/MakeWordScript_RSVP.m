%script to make letters and words for new display
displayName = 'default'; 

%% letters
[letterParams, letterImgs, letterStruct] = MakeLetters(displayName);

%% words
RSVP_MakeWordImages(displayName);

